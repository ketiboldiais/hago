export function ReturnSmaller(x: number, y: number) {
  if (x < y) {
    return x;
  } else {
    return y;
  }
}

export function ReturnLarger(x: number, y: number) {
  if (x > y) {
    return x;
  } else {
    return y;
  }
}
