import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs';
import { DoublyLinkedList, LinkedListProps } from '../src';

<Meta
  title="Basic Data Structures/Doubly-linked Lists"
  component={DoublyLinkedList}
/>

# Doubly-linked Lists

Doubly-linked lists can be rendered with the doubly-linked list component:

<DoublyLinkedList />
