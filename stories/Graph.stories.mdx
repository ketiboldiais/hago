import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs';
import { Graph, GraphProps } from '../src';

<Meta
  title="Graphs/Basic Graph"
  component={Graph}
/>

# Graphs

Graphs are rendered with the `Graph` component:

<Graph 
  data={[
    {link: [1,2]},
    {link: [1,3]},
    {link: [1,1], weight:5},
  ]}
  straightEdge={false}
/>
