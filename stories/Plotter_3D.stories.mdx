import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs';
import { Plot3D } from '../src/Plot3D/Plot3D';

<Meta title="Plotters/3D Plots" component={Plot3D} />

# Plot 3D

For more interactive parametric functions, we can use the `Plot3D` module:

<Plot3D />
