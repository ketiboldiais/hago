import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs';
import { Histogram, HistogramProps } from '../src';
import './assets/style.css';

<Meta title="Plotters/Histogram" component={Histogram} />

# Histograms

The `<Histogram/>` component can render basic histograms:

<Histogram />
