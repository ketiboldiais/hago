import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs';
import { Plot, PlotProps, Maths } from '../../../src';

<Meta title="Plot/Gallery/Tangent Function" component={Plot} />

# Tangent Function

The tangent function:

<Plot data={[{ f: (x) => Math.tan(x) }]} />
