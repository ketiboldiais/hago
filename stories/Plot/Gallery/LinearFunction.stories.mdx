import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs';
import { Plot, PlotProps, Maths } from '../../../src';

<Meta title="Plot/Gallery/Linear Functions" component={Plot} />

# Linear Functions

Linear functions are functions of the form <Maths val={"f(x)=a+bx"} block={false}/>, where <Maths val={"a,b,x \\in \\R"} block={false}/>. Below are a few examples of linear functions.

```javascript
<Plot data={[{ f: (x) => x + 1 }]} domain={[-5, 10]} range={[-5, 10]} />
<Plot data={[{ f: (x) => 0.5 * x }]} domain={[0,10]} range={[0,10]}/>
<Plot data={[{ f: (x) => -3 * x }]} />
```

we get the result:

<Plot data={[{ f: (x) => x + 1 }]} domain={[-5, 10]} range={[-5, 10]} />
<Plot data={[{ f: (x) => 0.5 * x }]} domain={[0, 10]} range={[0, 10]} />
<Plot data={[{ f: (x) => -3 * x, id: 'f(x) = -3x' }]} />


## Shapes

<Plot data={[
	{s:'circle', r:2, xy:[4,4]},
	{s:'circle', r:2, xy:[5,4]},
]} id={'circ1'} domain={[0,10]} range={[0,10]}/>

## Discretization
<Plot data={[
	{f:(x)=>Math.cos(x), disc:50, r:2}
]}/>