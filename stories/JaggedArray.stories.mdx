import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs';
import { JaggedArray, JaggedArrayProps } from '../src';

<Meta title="Basic Data Structures/Jagged Arrays" component={JaggedArray} />

# Jagged Arrays

Jagged arrays can be rendered through the `<JaggedArray/>` component:

<JaggedArray />
