import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs';
import { HashTable, HashTableProps } from '../src';

<Meta title="Basic Data Structures/Hash Tables" component={HashTable} />

# Hash Tables

Hash tables can be rendered with the `<HashTable/>` component.

<HashTable />
