import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs';
import { Sequence, SequenceProps } from '../src';

<Meta title="Plotters/Sequence" component={Sequence} />

# Sequence Plotting

Hago's `Sequence` module provides a way to plot sequences.

<Sequence data={[{ f: (n) => n ** 2 }]} />
