import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs';
import './assets/style.css';
import { CartProd, CartProdProps } from '../src';

<Meta title="Statistics/CartProd" component={CartProd} />

# Cartesian Product

The `CartProd` module renders a 2-way table, a common medium for diagramming
sample spaces in discrete probability.

<CartProd/>