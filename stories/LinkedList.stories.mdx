import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs';
import { LinkedList, LinkedListProps } from '../src';

<Meta
  title="Basic Data Structures/Singly-linked Lists"
  component={LinkedList}
/>

# Singly-linked Lists

Singly-linked lists are used extensively in modern programming. Hago can render the singly-linked list with the `LinkedList` component.

<LinkedList />
