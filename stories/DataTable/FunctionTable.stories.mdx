import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs';
import { DataTable, DataTableProps } from '../../src';
import '../assets/style.css';

<Meta title="DataTable/Function Tables" component={DataTable} />

# Function Tables

Function tables can be rendered by passing a `FunctionTest` object to the data field. For example, writing:

```javascript
<DataTable
  data={{
    f: [(x) => x ** 2, (x) => x ** 3],
    fLabels: ['f(x) = x^2', 'f(x) = x^3'],
    domain: [-2, 2],
    step: 0.25,
  }}
  latex={true}
/>
```

renders the table:

<DataTable
  data={{
    f: [(x) => x ** 2, (x) => x ** 3],
    fLabels: ['f(x) = x^2', 'f(x) = x^3'],
    domain: [-2, 2],
    step: 0.25,
  }}
  latex={true}
/>
