import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs';
import { Stack, StackProps } from '../src/Stack/Stack';

<Meta title="Basic Data Structures/Stack" component={Stack} />

# Stack

Hago visualizes stacks with the `Stack` module:

<Stack
  data={[
    '',
    '',
    { val: 'x', ptr: "%rsp", ant: 'stack'},
  ]}
/>
