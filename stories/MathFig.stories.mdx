import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs';
import { MathFig, MathFigProps, Maths } from '../src';

<Meta title="Plotters/Equation Blocks" component={MathFig} />

# Equation Blocks

The `MathFig` component provides a way to quickly render blocks of equations.

<MathFig/>