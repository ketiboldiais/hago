import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs';
import { PriorityQueue, PriorityQueueProps } from '../src';

<Meta title="Basic Data Structures/Priority Queues" component={PriorityQueue} />

# Priority Queues

Priority queues are rendered with the `<PriorityQueue />` component:

<PriorityQueue />
